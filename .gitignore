node_modules/
dist/
*.tsbuildinfo
vite.config.js
vite.config.d.ts
.vercel

# Local environment variables (Supabase URL/key). Configured per-environment
# (locally and in Vercel); never commit real credentials.
.env
.env.local
.env.*.local
